# PostgreSQL credentials (override config.yaml)
PGUSER=postgres
PGPASSWORD=
PGHOST=
PGPORT=
PGDATABASE=

# Vision LLM API key for image table extraction (OpenRouter by default)
OPENROUTER_API_KEY=sk-or-...
